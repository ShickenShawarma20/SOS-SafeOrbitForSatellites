{
  "name": "sos-safeorbitforsattelites",
  "version": "1.0.0",
  "description": "SOS · SafeOrbitForSattelites — Satellite Collision Avoidance System",
  "main": "server/dist/index.js",
  "scripts": {
    "start": "node server/dist/index.js",
    "dev": "npx tsx server/src/index.ts",
    "build": "node -e \"require('fs').existsSync('server/dist/index.js') ? console.log('server/dist is prebuilt, skipping tsc') : (require('child_process').execSync('tsc -p server/tsconfig.json', { stdio: 'inherit' }))\"",
    "build:ts": "tsc -p server/tsconfig.json",
    "serve:static": "npx --yes serve -l 3000 ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "uuid": "^10.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.5.12",
    "puppeteer-core": "^25.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}