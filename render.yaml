services:
  - type: web
    name: sos-backend
    runtime: node
    plan: free
    # server/dist is generated and excluded from Git, so compile it before npm start.
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
